extends layout

block content
  h3.category-header=category.name
  h5.category-description=category.descriptoin
  #threads
    each thread in threads
      .card.thread
        .card-body
          a.card-title(href='/thread/' + thread.id)= thread.title